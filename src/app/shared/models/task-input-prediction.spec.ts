/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {TaskInputPrediction} from './task-input-prediction';

describe('TaskInputPrediction', () => {
  it('should create an instance', () => {
    expect(new TaskInputPrediction()).toBeTruthy();
  });
});
